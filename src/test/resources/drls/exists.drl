package example2;

declare Exists
    json : Json
    id   : Integer
end

rule "exists"
    when
        $e : Exists( ($j : json.a.b exists) && ($p : id != 1), $i : id exists)
    then
        $e.setId(1);
end